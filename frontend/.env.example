# Local dev
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Production (required): must be your real backend HTTPS URL or images stay on localhost and break.
# Vercel — add this on the *frontend* project (must end with /api):
# NEXT_PUBLIC_API_URL=https://your-backend.vercel.app/api
