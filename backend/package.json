{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "build": "node --check src/server.js && node --check src/app.js && node --check api/index.js"
  },
  "dependencies": {
    "cloudinary": "latest",
    "compression": "^1.8.1",
    "cors": "latest",
    "dotenv": "latest",
    "express": "latest",
    "mongoose": "latest",
    "multer": "latest"
  },
  "devDependencies": {
    "nodemon": "latest"
  }
}
